spring:
  cloud:
    nacos:
      discovery:
        username: nacos
        password: nacos
        server-addr: 192.172.0.203:8848
        group: yuyang
    sentinel:
      transport:
        dashboard: 127.0.0.1:8858

logging:
  pattern:
    console: '%clr(%5level) %clr([%15thread]){faint} %clr(%-40.40logger{39}){cyan} %clr(:){faint} %m%n%wEx'
